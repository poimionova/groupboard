[build]
builder = "NIXPACKS"
buildCommand = "npm install --legacy-peer-deps && npx vite build"

[deploy]
startCommand = "sh -c 'serve -s dist -l ${PORT:-3000}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
